<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Couleur de fond de l'EditText (peut être transparent ou toute autre couleur) -->
    <corners android:radius="8dp"/> <!-- Pour arrondir les coins (si vous le souhaitez) -->
    <stroke
        android:color="@android:color/holo_blue_dark" android:width="2dp"/> <!-- Largeur de la bordure -->
</shape>
